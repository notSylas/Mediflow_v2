openai>=1.40.0
pydantic>=2.6.0
PyMuPDF>=1.24.0
python-dotenv>=1.0.0
fastapi>=0.110.0
uvicorn>=0.29.0
python-multipart>=0.0.9
# Cloud Run Job writes results straight back to Postgres.
psycopg[binary]>=3.1.0
# Hand-drawn diagram detection (models/best.pt, YOLO11 single-class).
# CPU-only torch: the CUDA wheels are ~2.5 GB and a Cloud Run Job has no GPU.
# Install torch from the CPU index first — see Dockerfile.analyzer.
ultralytics>=8.3.0
pillow>=10.0.0
